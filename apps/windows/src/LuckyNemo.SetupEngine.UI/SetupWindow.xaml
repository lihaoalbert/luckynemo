<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LuckyNemo.SetupEngine.UI.SetupWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="LuckyNemo Setup">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar drag region with icon + text -->
        <Grid x:Name="TitleBarDrag" Grid.Row="0" Padding="12,0,0,0">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Image Source="ms-appx:///LuckyNemo.SetupEngine.UI/Assets/Setup/LuckyNemoMascot.png" Width="20" Height="20"
                       AutomationProperties.AccessibilityView="Raw" />
                <TextBlock Text="LuckyNemo Setup" FontSize="13" Opacity="0.85"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Frame x:Name="RootFrame" Grid.Row="1" Navigated="RootFrame_Navigated" NavigationFailed="RootFrame_NavigationFailed" />
    </Grid>

</Window>
